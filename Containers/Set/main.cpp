#include<iostream>
#include<set>
using namespace std;

int main()
{
    //Creation of a set
    set<int>st;

    //Insertion in a set
    st.insert(10);
    st.insert(20);
    st.insert(30);
    st.insert(40);

    //It will store it in SORTED ORDER and DUPLICATES will be DELETED.
}